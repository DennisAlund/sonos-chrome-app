define(function (require) {
        "use strict";

        return {
            util: require("./util"),
            messaging: require("./messaging"),
            definitions: require("./definitions")
        };
    }
);
